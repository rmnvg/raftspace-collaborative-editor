# validation

Zod schemas for request/response payloads (document create/rename, sharing,
file import). Shared between client forms and server route handlers so
validation logic isn't duplicated.

See `documents.ts`.
